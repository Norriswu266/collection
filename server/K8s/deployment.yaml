apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      # imagePullSecrets:
      #   - name: acr-auth
      containers:
        - name: api
          image: norrisacr.azurecr.io/api:53ea0225813be717abd3963c6d7d43976334d1cc
          ports:
            - containerPort: 3000
