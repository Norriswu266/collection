apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      # imagePullSecrets:
      #   - name: acr-auth
      containers:
        - name: api
          image: norrisacr.azurecr.io/api:f292fa402b6e30b55a1befab03881b758ab8a75c
          ports:
            - containerPort: 3000
